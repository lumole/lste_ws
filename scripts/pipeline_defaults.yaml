# Default parameters for one-click pipeline launch.
# You can override any of these via environment variables or by editing this file.
# All paths support the $WS variable (auto-detected workspace root).
# Set LSTE_WS env var to override the workspace auto-detection.

# WS is auto-detected from the script location, no need to set here
# SESSION: lste

# World and task (paths relative to $WS)
WORLD: worlds/topo3.0_catch_mode/session_test/env04.world
TASK_JSON: model/Data_exchange/vlm_prompt/lab/yellow_cup.json
TASK_ID: yellow_cup

# Models and configs (paths relative to $WS)
VLLM_URL: http://localhost:8000/v1
VLLM_MODEL: model/MiniCPM/OpenBMB/MiniCPM4-0___5B
ACCESS_TOPO_CONFIG: src/lste_topo_access/topo_tree/cfgs/access_topo.yaml
ACCESS_TOPO_CONFIG_PASS: src/lste_topo_access/topo_tree/cfgs/access_topo_pass.yaml
ACCESS_TOPO_CONFIG_SUS_C: src/lste_topo_access/topo_tree/cfgs/access_topo_sus_c.yaml
ACCESS_TOPO_TEST_NAME: 2601
ACCESS_TOPO_RUN_NAME: topo_3.0_catch_mode
GP_FRONTIER_RVIZ: src/lste_topo_access/launch/gp_frontier.rviz

# State / Goal tuning
SUSPICIOUS_WINDOW: 5
FOLLOW_LOCKED_DONE_TIME: 4.0

# Frontier logging toggle (true/false)
FRONTIER_LOG: false

# Gazebo GUI toggle (true/false)
GUI: true
